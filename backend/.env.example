PORT=8080
DATABASE_URL=postgresql://user:password@localhost:5432/nutritionai
GEMINI_API_KEY=your_gemini_api_key_here
JWT_SECRET=your_jwt_secret_key_here

# Firebase Configuration (Optional - for Firestore)
# If set, the app will use Firestore instead of PostgreSQL
FIREBASE_PROJECT_ID=your-firebase-project-id
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}
# Or use Application Default Credentials (automatic in Cloud Run)

# Database type: 'postgres' or 'firestore' (auto-detected if not set)
# DATABASE_TYPE=firestore
